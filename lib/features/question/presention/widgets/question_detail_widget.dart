
import 'package:flutter/material.dart';
import 'package:questionsapp/features/question/domain/entities/question.dart';

class QuestionDetailWidget extends StatelessWidget {
  Question question;
   QuestionDetailWidget({Key? key,required this.question}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
